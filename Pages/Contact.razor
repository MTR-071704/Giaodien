@page "/Contact"
@inject IJSRuntime JSRuntime

<PageTitle>Liên Hệ - HealthScheduler</PageTitle>

@* ===== HEADER VÀ THANH ĐIỀU HƯỚNG (GIỮ NGUYÊN ĐỂ ĐỒNG BỘ) ===== *@
<div class="header-top">
    <div class="container d-flex justify-content-between align-items-center">
        <div class="contact-info">
            <a href="tel:+840387152537"><i class="fa-solid fa-phone"></i> (+84) 0387152537</a>
            <span class="separator">|</span>
            <a href="mailto:PhucPhuong@gmail.com"><i class="fa-solid fa-envelope"></i> PhucPhuong@gmail.com</a>
        </div>
        <div class="social-icons">
            <a href="#" aria-label="Facebook"><i class="fab fa-facebook-f"></i></a>
            <a href="#" aria-label="Twitter"><i class="fab fa-twitter"></i></a>
            <a href="#" aria-label="LinkedIn"><i class="fab fa-linkedin-in"></i></a>
        </div>
    </div>
</div>

<nav class="navbar navbar-expand-lg main-nav" id="mainNav">
    <div class="container">
        <a class="navbar-brand" href="/">
            <img src="/images/Logo 3.png" alt="Logo" class="header-logo" />
            <span class="brand-name-text">PHUCPHUONG</span>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item"><NavLink class="nav-link" href="/">Trang chủ</NavLink></li>
                <li class="nav-item"><NavLink class="nav-link" href="/services">Dịch vụ</NavLink></li>
                <li class="nav-item"><NavLink class="nav-link" href="/doctors">Bác sĩ</NavLink></li>
                <li class="nav-item"><NavLink class="nav-link" href="/about">Về chúng tôi</NavLink></li>
                <li class="nav-item"><NavLink class="nav-link active" href="/contact">Liên hệ</NavLink></li>
            </ul>
            <a href="/#appointment-section" class="btn btn-primary-custom ms-lg-3">Đặt lịch hẹn</a>
        </div>
    </div>
</nav>

@* ===== NỘI DUNG CHÍNH CỦA TRANG "LIÊN HỆ" (NÂNG CẤP) ===== *@
<div class="contact-page-body">
    <!-- HERO SECTION -->
    <section class="about-hero">
        <div class="container text-center">
            <h1 class="about-hero-title animate-on-load fade-in-up">Liên Hệ Với Chúng Tôi</h1>
            <p class="about-hero-subtitle animate-on-load fade-in-up" data-delay="200">Chúng tôi luôn sẵn sàng lắng nghe và giải đáp mọi thắc mắc của bạn.</p>
        </div>
    </section>

    <!-- CONTACT INFO CARDS SECTION -->
    <section class="contact-info-section">
        <div class="container">
            <div class="row g-4">
                <div class="col-lg-4 animate-on-scroll fade-in-up">
                    <div class="info-card">
                        <div class="info-icon"><i class="fa-solid fa-location-dot"></i></div>
                        <h3 class="info-title">Địa Chỉ</h3>
                        <p class="info-text">99 Hồ Đắc Di, An Cựu, TP. Huế, Việt Nam</p>
                    </div>
                </div>
                <div class="col-lg-4 animate-on-scroll fade-in-up" data-delay="200">
                    <div class="info-card">
                        <div class="info-icon"><i class="fa-solid fa-envelope-open-text"></i></div>
                        <h3 class="info-title">Địa Chỉ Email</h3>
                        <p class="info-text">
                            <a href="mailto:PhucPhuong@gmail.com" class="info-link">PhucPhuong@gmail.com</a>
                        </p>
                    </div>
                </div>
                <div class="col-lg-4 animate-on-scroll fade-in-up" data-delay="400">
                    <div class="info-card">
                        <div class="info-icon"><i class="fa-solid fa-phone-volume"></i></div>
                        <h3 class="info-title">Điện Thoại</h3>
                        <p class="info-text">
                            <a href="tel:+840387152537" class="info-link">(+84) 0387152537</a>
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- CONTACT FORM & MAP SECTION -->
    <section class="contact-form-map-section">
        <div class="container">
            <div class="row g-0 align-items-stretch">
                <div class="col-lg-6 form-col animate-on-scroll fade-in-left">
                    <div class="contact-form-wrapper">
                        <div class="section-header text-start mb-4">
                            <span class="hero-tagline">GỬI LỜI NHẮN</span>
                            <h2 class="section-title">Chúng Tôi Có Thể Giúp Gì Cho Bạn?</h2>
                        </div>
                        <form>
                            <div class="row">
                                <div class="col-md-6 mb-4">
                                    <input type="text" class="form-control" placeholder="Họ và tên của bạn" required>
                                </div>
                                <div class="col-md-6 mb-4">
                                    <input type="email" class="form-control" placeholder="Email của bạn" required>
                                </div>
                                <div class="col-12 mb-4">
                                    <input type="text" class="form-control" placeholder="Chủ đề" required>
                                </div>
                                <div class="col-12 mb-4">
                                    <textarea class="form-control" rows="6" placeholder="Nội dung lời nhắn..." required></textarea>
                                </div>
                                <div class="col-12">
                                    <button type="submit" class="btn btn-primary-custom w-100">Gửi Lời Nhắn</button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
                <div class="col-lg-6 map-col animate-on-scroll fade-in-right" data-delay="200">
                    <iframe src="https://maps.google.com/maps?q=99%20H%E1%BB%93%20%C4%90%E1%BA%AFc%20Di,%20An%20C%E1%BB%B1u,%20Hu%E1%BA%BF&t=&z=17&ie=UTF8&iwloc=&output=embed"
                            width="100%"
                            height="100%"
                            style="border:0; min-height: 400px;"
                            allowfullscreen=""
                            loading="lazy"
                            referrerpolicy="no-referrer-when-downgrade">
                    </iframe>
                </div>
            </div>
        </div>
    </section>

    <!-- FAQ SECTION -->
    <section class="faq-section">
        <div class="container">
            <div class="text-center section-header animate-on-scroll fade-in-up">
                <span class="hero-tagline">GIẢI ĐÁP THẮC MẮC</span>
                <h2 class="section-title">Câu Hỏi Thường Gặp</h2>
            </div>
            <div class="faq-accordion mx-auto">
                @for (int i = 0; i < faqs.Count; i++)
                {
                    var index = i;
                    <div class="faq-item animate-on-scroll fade-in-up" data-delay="@(index * 100)">
                        <button class="faq-question" @onclick="() => ToggleFaq(index)">
                            <span>@faqs[index].Question</span>
                            <i class="fa-solid @(faqOpenState.GetValueOrDefault(index) ? "fa-minus" : "fa-plus")"></i>
                        </button>
                        @if (faqOpenState.GetValueOrDefault(index))
                        {
                            <div class="faq-answer">
                                <p>@faqs[index].Answer</p>
                            </div>
                        }
                    </div>
                }
            </div>
        </div>
    </section>

    <!-- FOOTER -->
    <footer class="site-footer">
        <div class="container">
            <div class="row">
                <div class="col-lg-4 col-md-6">
                    <div class="footer-widget">
                        <a class="footer-brand" href="/">
                            <img src="/images/Logo 3.png" alt="Logo" class="footer-logo" />
                            <span class="brand-name-text">PHUCPHUONG</span>
                        </a>
                        <p class="footer-about">Nền tảng y tế số hàng đầu, mang lại giải pháp chăm sóc sức khỏe toàn diện, thông minh và tiện lợi cho mọi người.</p>
                    </div>
                </div>
                <div class="col-lg-2 col-md-6">
                    <div class="footer-widget">
                        <h5 class="widget-title">Liên kết nhanh</h5>
                        <ul class="list-unstyled footer-links">
                            <li><a href="/about">Về chúng tôi</a></li>
                            <li><a href="/services">Dịch vụ</a></li>
                            <li><a href="/doctors">Bác sĩ</a></li>
                            <li><a href="/contact">Liên hệ</a></li>
                        </ul>
                    </div>
                </div>
                <div class="col-lg-3 col-md-6">
                    <div class="footer-widget">
                        <h5 class="widget-title">Dịch vụ chính</h5>
                        <ul class="list-unstyled footer-links">
                            <li><a href="#">Khoa Tim Mạch</a></li>
                            <li><a href="#">Khoa Nhi</a></li>
                            <li><a href="#">Khoa Răng Hàm Mặt</a></li>
                            <li><a href="#">Khám tổng quát</a></li>
                        </ul>
                    </div>
                </div>
                <div class="col-lg-3 col-md-6">
                    <div class="footer-widget">
                        <h5 class="widget-title">Thông tin liên hệ</h5>
                        <ul class="list-unstyled footer-contact">
                            <li><i class="fa-solid fa-location-dot"></i> 99 Hồ Đắc Di, An Cựu, TP. Huế</li>
                            <li><i class="fa-solid fa-phone"></i> (+84) 0387152537</li>
                            <li><a href="mailto:PhucPhuong@gmail.com" style="color: rgba(255,255,255,0.7); text-decoration: none;"><i class="fa-solid fa-envelope"></i> PhucPhuong@gmail.com</a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
        <div class="footer-bottom">
            <div class="container text-center">
                <p>&copy; @DateTime.Now.Year PHUCPHUONG. All Rights Reserved. </p>
            </div>
        </div>
    </footer>
</div>

<style>
    /* Reset and Base Styles (Copied from Index for consistency) */
    :root {
        --primary-color: #0fabf1;
        --secondary-color: #012548;
        --text-color: #555;
        --heading-color: #012548;
        --light-bg-color: #f8faff;
        --white-color: #ffffff;
        --border-color: #e9e9e9;
    }

    .contact-page-body {
        font-family: 'Be Vietnam Pro', sans-serif;
        background-color: var(--white-color);
    }

    .hero-tagline {
        color: var(--primary-color);
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .section-header .section-title {
        font-size: 2.8rem;
        font-weight: 700;
        color: var(--heading-color);
    }

    /* Animation Keyframes */
    @@keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(40px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @@keyframes fadeInLeft {
        from {
            opacity: 0;
            transform: translateX(-40px);
        }

        to {
            opacity: 1;
            transform: translateX(0);
        }
    }

    @@keyframes fadeInRight {
        from {
            opacity: 0;
            transform: translateX(40px);
        }

        to {
            opacity: 1;
            transform: translateX(0);
        }
    }

    /* Animation Classes */
    .animate-on-load, .animate-on-scroll {
        opacity: 0;
    }

        .animate-on-scroll.is-visible, .animate-on-load.is-visible {
            animation-duration: 0.8s;
            animation-fill-mode: forwards;
            animation-timing-function: cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

    .fade-in-up.is-visible {
        animation-name: fadeInUp;
    }

    .fade-in-left.is-visible {
        animation-name: fadeInLeft;
    }

    .fade-in-right.is-visible {
        animation-name: fadeInRight;
    }

    /* Hero Section (Reused from About page) */
    .about-hero {
        padding: 100px 0;
        background: linear-gradient(to right, var(--secondary-color), var(--primary-color));
        color: var(--white-color);
    }

    .about-hero-title {
        font-size: 4rem;
        font-weight: 800;
        text-shadow: 2px 2px 10px rgba(0,0,0,0.2);
    }

    .about-hero-subtitle {
        font-size: 1.25rem;
        max-width: 700px;
        margin: 1rem auto 0;
        opacity: 0.9;
    }

    .contact-info-section {
        padding: 100px 0;
    }

    .info-card {
        background-color: var(--white-color);
        padding: 40px;
        border-radius: 15px;
        text-align: center;
        height: 100%;
        transition: all 0.3s ease;
        border: 1px solid var(--border-color);
        box-shadow: 0 10px 30px -15px rgba(0,0,0,0.05);
    }

        .info-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(15, 171, 241, 0.2);
            border-color: var(--primary-color);
        }

    .info-icon {
        font-size: 3rem;
        color: var(--primary-color);
        margin-bottom: 20px;
        transition: color 0.3s ease;
    }

    .info-title {
        font-size: 1.5rem;
        margin-bottom: 10px;
        color: var(--heading-color);
        transition: color 0.3s ease;
    }

    .info-text {
        font-size: 1rem;
        color: var(--text-color);
    }

    .info-link {
        color: var(--text-color);
        text-decoration: none;
        transition: color 0.3s ease;
    }

    .contact-form-map-section {
        padding: 0 0 100px 0;
    }

    .contact-form-wrapper {
        background-color: var(--light-bg-color);
        padding: 50px;
        height: 100%;
    }

    .map-col {
        background-color: #e0e0e0;
    }

    .contact-form-wrapper .form-control {
        height: 55px;
        border-radius: 5px;
        background-color: var(--white-color);
        border: 1px solid var(--border-color);
        padding: 10px 20px;
    }

    .contact-form-wrapper textarea.form-control {
        height: auto;
    }

    .contact-form-wrapper .form-control:focus {
        border-color: var(--primary-color);
        box-shadow: 0 0 0 0.25rem rgba(13, 110, 253, 0.25);
    }

    /* FAQ Section */
    .faq-section {
        padding: 100px 0;
        background-color: var(--white-color);
    }

    .faq-accordion {
        max-width: 800px;
    }

    .faq-item {
        border-bottom: 1px solid var(--border-color);
    }

    .faq-question {
        width: 100%;
        padding: 20px 0;
        display: flex;
        justify-content: space-between;
        align-items: center;
        text-align: left;
        background: none;
        border: none;
        font-size: 1.2rem;
        font-weight: 600;
        color: var(--heading-color);
        cursor: pointer;
        transition: color 0.3s ease;
    }

        .faq-question:hover {
            color: var(--primary-color);
        }

        .faq-question i {
            font-size: 1rem;
            transition: transform 0.3s ease;
        }

    .faq-answer {
        padding: 0 0 20px 0;
        color: var(--text-color);
        line-height: 1.7;
        animation: fadeInDown 0.4s ease;
    }

    @@keyframes fadeInDown {
        from {
            opacity: 0;
            transform: translateY(-10px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }


    /* Copied Styles from Index */
    .header-top {
        background-color: #f8f9fa;
        padding: 10px 0;
        font-size: 14px;
        border-bottom: 1px solid var(--border-color);
    }

    .contact-info a {
        color: var(--text-color);
        text-decoration: none;
        margin-right: 15px;
        transition: color 0.3s ease;
    }

        .contact-info a:hover {
            color: var(--primary-color);
        }

    .contact-info i {
        color: var(--primary-color);
        margin-right: 5px;
    }

    .separator {
        color: #ddd;
        margin-right: 15px;
    }

    .social-icons a {
        color: var(--text-color);
        margin-left: 15px;
        transition: color 0.3s ease;
    }

        .social-icons a:hover {
            color: var(--primary-color);
        }

    .main-nav {
        background-color: var(--white-color);
        box-shadow: 0 2px 15px rgba(0,0,0,0.05);
        padding: 10px 0;
        transition: all 0.3s ease;
    }

    .navbar-brand {
        display: flex;
        align-items: center;
    }

    .header-logo {
        height: 50px;
        margin-right: 10px;
    }

    .brand-name-text {
        font-family: 'Lora', serif;
        font-size: 1.8rem;
        font-weight: 700;
        color: var(--heading-color);
    }

    .main-nav .nav-link {
        color: var(--heading-color);
        font-weight: 500;
        margin: 0 10px;
        padding: 8px 15px;
        border-radius: 5px;
        transition: all 0.3s ease;
        position: relative;
    }

        .main-nav .nav-link::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 0;
            height: 2px;
            background-color: var(--primary-color);
            transition: width 0.3s ease;
        }

        .main-nav .nav-link:hover::after, .main-nav .nav-link.active::after {
            width: 50%;
        }

        .main-nav .nav-link:hover, .main-nav .nav-link.active {
            color: var(--primary-color);
            background-color: transparent;
        }

    .btn-primary-custom {
        background-image: linear-gradient(to right, var(--primary-color) 0%, #0ab3e4 50%, var(--primary-color) 100%);
        background-size: 200% auto;
        color: var(--white-color);
        border: none;
        padding: 12px 30px;
        border-radius: 50px;
        font-weight: 600;
        transition: all 0.4s ease;
        text-decoration: none;
        box-shadow: 0 4px 15px rgba(15, 171, 241, 0.3);
    }

        .btn-primary-custom:hover {
            background-position: right center;
            color: var(--white-color);
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(15, 171, 241, 0.4);
        }

    .site-footer {
        background-color: var(--secondary-color);
        color: rgba(255,255,255,0.7);
        padding: 80px 0 0;
    }

    .footer-widget .widget-title {
        color: var(--white-color);
        font-size: 1.2rem;
        margin-bottom: 20px;
    }

    .footer-brand {
        display: flex;
        align-items: center;
        text-decoration: none;
        margin-bottom: 15px;
    }

    .footer-logo {
        height: 40px;
    }

    .footer-brand .brand-name-text {
        color: var(--white-color);
        margin-left: 10px;
    }

    .footer-about {
        line-height: 1.8;
    }

    .footer-links li {
        margin-bottom: 10px;
    }

    .footer-links a {
        color: rgba(255,255,255,0.7);
        text-decoration: none;
        transition: all 0.3s ease;
    }

        .footer-links a:hover {
            color: var(--primary-color);
            padding-left: 5px;
        }

    .footer-contact li {
        display: flex;
        align-items: flex-start;
        margin-bottom: 15px;
    }

    .footer-contact i {
        color: var(--primary-color);
        margin-right: 15px;
        margin-top: 5px;
    }

    .footer-bottom {
        background-color: #001c38;
        padding: 20px 0;
        margin-top: 60px;
    }

        .footer-bottom p {
            margin: 0;
            color: rgba(255,255,255,0.5);
            font-size: 0.9rem;
        }

    .section-header.text-start {
        text-align: left !important;
    }
</style>


@code {
    private List<FaqItem> faqs = new List<FaqItem>();
    private Dictionary<int, bool> faqOpenState = new Dictionary<int, bool>();

    protected override void OnInitialized()
    {
        faqs.Add(new FaqItem { Question = "Làm thế nào để đặt lịch hẹn?", Answer = "Bạn có thể đặt lịch hẹn trực tiếp qua website bằng cách nhấn vào nút 'Đặt lịch hẹn' ở đầu trang, hoặc gọi đến số hotline (+84) 0387152537 để được hỗ trợ." });
        faqs.Add(new FaqItem { Question = "Bệnh viện có chấp nhận thanh toán bảo hiểm không?", Answer = "Có, chúng tôi chấp nhận nhiều loại bảo hiểm y tế khác nhau. Vui lòng liên hệ bộ phận lễ tân để biết thêm chi tiết về loại bảo hiểm của bạn." });
        faqs.Add(new FaqItem { Question = "Giờ làm việc của bệnh viện như thế nào?", Answer = "Chúng tôi làm việc từ 8:00 - 19:00 từ Thứ Hai đến Thứ Sáu, và từ 9:00 - 17:00 vào Thứ Bảy. Chủ Nhật bệnh viện đóng cửa." });
        faqs.Add(new FaqItem { Question = "Tôi cần chuẩn bị gì trước khi đến khám?", Answer = "Bạn vui lòng mang theo giấy tờ tùy thân (CMND/CCCD), thẻ bảo hiểm y tế (nếu có), và các kết quả xét nghiệm, đơn thuốc cũ liên quan đến tình trạng sức khỏe của bạn để bác sĩ tiện theo dõi." });
    }

    private void ToggleFaq(int index)
    {
        if (faqOpenState.ContainsKey(index))
        {
            faqOpenState[index] = !faqOpenState[index];
        }
        else
        {
            faqOpenState.Add(index, true);
        }
        StateHasChanged();
    }

    public class FaqItem
    {
        public string Question { get; set; }
        public string Answer { get; set; }
    }

    private string script = @"
        window.initContactPageAnims = function() {
            const observerOptions = {
                root: null,
                rootMargin: '0px',
                threshold: 0.1
            };

            const observer = new IntersectionObserver((entries, obs) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        const delay = entry.target.dataset.delay || 0;
                        setTimeout(() => {
                            entry.target.classList.add('is-visible');
                            if (entry.target.classList.contains('fade-in-up')) {
                                entry.target.style.animationName = 'fadeInUp';
                            } else if (entry.target.classList.contains('fade-in-left')) {
                                entry.target.style.animationName = 'fadeInLeft';
                            } else if (entry.target.classList.contains('fade-in-right')) {
                                entry.target.style.animationName = 'fadeInRight';
                            }
                        }, parseInt(delay));
                        obs.unobserve(entry.target);
                    }
                });
            }, observerOptions);

            document.querySelectorAll('.animate-on-scroll, .animate-on-load').forEach(el => {
                 if (el.classList.contains('animate-on-load')) {
                    const delay = el.dataset.delay || 0;
                    setTimeout(() => {
                       el.classList.add('is-visible');
                       if (el.classList.contains('fade-in-up')) el.style.animationName = 'fadeInUp';
                       if (el.classList.contains('fade-in-left')) el.style.animationName = 'fadeInLeft';
                       if (el.classList.contains('fade-in-right')) el.style.animationName = 'fadeInRight';
                    }, parseInt(delay));
                } else {
                    observer.observe(el);
                }
            });
        };";

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await JSRuntime.InvokeVoidAsync("eval", script);
            await JSRuntime.InvokeVoidAsync("initContactPageAnims");
        }
    }
}
